# Copy to .env and fill in real values locally. Never commit .env — see .gitignore.
# Anything prefixed VITE_ is bundled into the public JS and is visible to every
# visitor — never put secrets here (see docs/SECURITY_ANALYSIS.md, F4).

VITE_API_URL=http://localhost:5000/api
